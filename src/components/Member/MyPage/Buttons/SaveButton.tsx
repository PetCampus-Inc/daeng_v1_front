import BackgroundButton from "components/common/Button/BackgroundButton";

const SaveButton = () => {
  return (
    <BackgroundButton backgroundColor="white" buttonBackgroundColor="primaryColor" disabled>
      수정 완료
    </BackgroundButton>
  );
};

export default SaveButton;
